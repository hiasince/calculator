
중앙 집중형 시스템


분산 버전 관리 시스템

- 모든 컴퓨터가 저장소를 가지고 있는 형태
- 클라이언트, 서버 중 하나에 문제가 생겨도 다른 클라이언트를 통해 복구

넘나 졸리다.....

Git = 리눅스 커널 개발 시 개발

델타가 아니라 스냅샷 (각 파일의 변화를 시간 순으로 관리)
거의 모든 명령을 로컬에서 실행 (서버 없이 가능한 기능 많음)
무결성 : 체크섬(해시)를 구하고 체크섬으로 데이터를 관리

관리가 되고 있는 상태 : commited, modifed, Supqer

README를 생성하지 않으면, 저장소는 생성되지 않는다.
실제로 푸시를 해야 생긴다.

관리가 되고 있지 않는 상태 : untracked - (add) -> Staged
			Stage - (commit) -> unmodified
			unmodified - (modi) -> modified -> untracked

.gitignore란?

빌드 결과물 혹은 임시 파일 등은 굳이 버전관리를 할 필요가 없다..
.gitignore라는 텍스트 파일을 만들어서 무시할 파일 이름의 패턴을 적으면 무시됨.
cf) .gitignore 패턴을 자동으로 생성해주는 사이트 ( gitignore.io )
내가 정하고 싶은 경우 (sourcetree에서 무시로 설정)
이미 커밋되어 있는 경우는 무시되지 않음

* 무시하는 것을 커스텀으로 하는 경우 정규표현식으로 하는가?
